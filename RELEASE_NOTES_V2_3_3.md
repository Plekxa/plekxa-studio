# Creator Studio v2.3.3
- Keeps the v2.3.2 revision workflow fixes.
- Normalises notification event types used by the shared Enterprise/Studio workflow so contract, commission, deliverable and certificate events render consistently.
