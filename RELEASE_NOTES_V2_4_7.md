# Creator Studio v2.4.7
- Removes unsafe application fallback that could write auth user UUID into creator_id.
- Requires the canonical creator_profiles.id when submitting applications.
- Avoids duplicate project intro text when summary and description are identical.
